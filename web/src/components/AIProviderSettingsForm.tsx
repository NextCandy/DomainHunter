import { useEffect, useState } from "react";
import type { AISettings, AISettingsInput } from "../lib/api";
import { api } from "../lib/api";
import { Spinner, useToast } from "./ui";

export function AIProviderSettingsForm({ settings, models, onSaved }: { settings: AISettings | null; models: string[]; onSaved: () => void }) {
  const [form, setForm] = useState<AISettingsInput>(emptySettings());
  const [saving, setSaving] = useState(false);
  const toast = useToast();
  useEffect(() => { if (settings) setForm({ provider: settings.provider, base_url: settings.base_url, model: settings.model, timeout_seconds: settings.timeout_seconds, concurrency: settings.concurrency, max_output_tokens: settings.max_output_tokens, daily_limit: settings.daily_limit, cache_ttl_seconds: settings.cache_ttl_seconds, enabled: settings.enabled }); }, [settings]);
  function set<K extends keyof AISettingsInput>(key: K, value: AISettingsInput[K]) { setForm((current) => ({ ...current, [key]: value })); }
  async function save() { setSaving(true); try { await api.p1.ai.saveSettings(form); toast("AI 设置已保存", "success"); onSaved(); } catch (err) { toast(err instanceof Error ? err.message : "AI 设置保存失败", "error"); } finally { setSaving(false); } }
  return <section className="card p-4"><div className="flex flex-wrap items-start justify-between gap-2"><div><h2 className="text-[14px] font-semibold">AI Provider 设置</h2><p className="mt-1 text-[12px] text-ink-muted">默认 DeepSeek；浏览器只提交配置，API 请求始终由 Go 后端发起。</p></div><span className="rounded-full border border-line px-2 py-0.5 text-[11px] text-ink-muted">{settings?.api_key_set ? `Key 已配置（${settings.key_source}）` : "未配置 Key"}</span></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><label className="text-[12px] text-ink-muted">Provider<input className="input mt-1" value={form.provider} readOnly /></label><label className="text-[12px] text-ink-muted">Base URL<input className="input mt-1" value={form.base_url} onChange={(event) => set("base_url", event.target.value)} /></label><label className="text-[12px] text-ink-muted">模型<select className="input mt-1" value={form.model} onChange={(event) => set("model", event.target.value)}>{Array.from(new Set([form.model, ...models])).map((model) => <option key={model} value={model}>{model}</option>)}</select></label><label className="text-[12px] text-ink-muted">API Key（只填新 Key）<input className="input mt-1" type="password" autoComplete="new-password" value={form.api_key ?? ""} onChange={(event) => set("api_key", event.target.value)} placeholder={settings?.api_key_set ? "已配置，留空不修改" : "不会回显或写入明文"} /></label><label className="text-[12px] text-ink-muted">超时（秒）<input className="input mt-1" type="number" min="5" max="300" value={form.timeout_seconds} onChange={(event) => set("timeout_seconds", Number(event.target.value))} /></label><label className="text-[12px] text-ink-muted">并发（1–4）<input className="input mt-1" type="number" min="1" max="4" value={form.concurrency} onChange={(event) => set("concurrency", Number(event.target.value))} /></label><label className="text-[12px] text-ink-muted">最大输出 token<input className="input mt-1" type="number" min="128" max="8192" value={form.max_output_tokens} onChange={(event) => set("max_output_tokens", Number(event.target.value))} /></label><label className="text-[12px] text-ink-muted">每日限额<input className="input mt-1" type="number" min="1" max="10000" value={form.daily_limit} onChange={(event) => set("daily_limit", Number(event.target.value))} /></label><label className="text-[12px] text-ink-muted">缓存 TTL（秒）<input className="input mt-1" type="number" min="300" max="2592000" value={form.cache_ttl_seconds} onChange={(event) => set("cache_ttl_seconds", Number(event.target.value))} /></label></div><label className="mt-3 flex items-center gap-2 text-[12px] text-ink-muted"><input type="checkbox" checked={form.enabled} onChange={(event) => set("enabled", event.target.checked)} />启用 AI Worker（仍受每日限额、重试和缓存保护）</label><p className="mt-2 text-[11px] text-ink-faint">保存 API Key 需要服务端配置 DOMAINHUNTER_SECRET_KEY；否则请使用 DOMAINHUNTER_AI_API_KEY 环境变量。</p><div className="mt-4 flex justify-end"><button type="button" className="btn btn-primary" onClick={() => void save()} disabled={saving}>{saving && <Spinner />}保存 AI 设置</button></div></section>;
}

function emptySettings(): AISettingsInput { return { provider: "deepseek", base_url: "https://api.deepseek.com", model: "deepseek-v4-flash", timeout_seconds: 30, concurrency: 1, max_output_tokens: 1200, daily_limit: 50, cache_ttl_seconds: 86400, enabled: false }; }
